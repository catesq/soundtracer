def matcher(file_path, plugin_name):
    """
    Match a file with a plugin.

    Args:
        file_path (str): The path to the file to match.
        plugin_name (str): The name of the plugin to use for matching.
    
    Returns:
        None
    """
    
    print(f"Matching file '{file_path}' with plugin '{plugin_name}'")